import React from 'react';

const ProfileScreen: React.FC = () => {
  return (
    <div className="w-full h-full flex flex-col items-center justify-center p-20 text-gray-500">
      {/* Trang cài đặt trống theo yêu cầu */}
    </div>
  );
};

export default ProfileScreen;
